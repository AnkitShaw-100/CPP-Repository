//Character pointer

#include <iostream>
using namespace std;

int main() {
    const char *ptr = "Hello, World!"; // Pointer to a string

    cout << "String at ptr: " << ptr << endl; // Prints the string

    return 0;
}
